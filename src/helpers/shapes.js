import {getRandomInt} from './functions';

export default class Shapes {
  constructor(width, offset, shape, weight) {
    this.width = weight;
    this.shape = shape;
    this.weight = weight;
    this.offset = offset;
  }



}
